# v0.9.6 post-release polish — working TODO

**Branch:** `v0.9.6-post-release-polish`
**Base commit:** `d3ba040` ("test: pin PIC10F320 image hashes")
**Lifecycle:** living document, **branch-only**. Update it as items land. When
every item is `DONE` or `WONTFIX`, delete this file and merge the branch to
`main`. It must never reach `main`.

## Where these items came from

A meta-review of the tree performed on 2026-07-29 immediately before the v0.9.6
release run, at commit `af6120a`, then **re-verified in full against `d3ba040`**
(the `pic320-test-build` / pinned-image-hash commit). Every claim below was
checked against the actual tree or actual tool output at `d3ba040` — none is
carried over unverified. Where a number is asserted, the command that produced
it is given so it can be re-derived after compaction.

Nothing here blocked the release. These are correctness, accuracy and
redundancy items, not defects in the shipped images.

---

## Working protocol (READ THIS — parallel agents)

Two or more agent instances may work this list concurrently.

### Status markers

Each item carries a `**Status:**` line. Allowed values:

| Marker | Meaning |
| --- | --- |
| `TODO` | unclaimed, available |
| `CLAIMED <who>` | someone is working it right now |
| `NEEDS-USER` | blocked: requires a `src/` firmware edit, which per `AGENTS.md` only the user may make |
| `DONE <commit>` | landed; record the short SHA |
| `WONTFIX <reason>` | deliberately declined; record why |

**Claim before you edit.** Set `CLAIMED <who>` and save this file *first*, so
the other instance sees it. Release the claim if you stop.

### Rules

1. **Do not edit `src/*.c` or `src/*.h`.** `AGENTS.md`: firmware source edits
   are the user's. Items needing one are marked `NEEDS-USER`; describe the
   change precisely and stop. Test suite and `Makefile` edits are allowed under
   the user's guidance.
2. **Do not commit.** The user does all git operations by hand. When an item is
   ready, say so; if asked, write `commit_msg.txt`.
3. **No "Co-Authored-By" or agent attribution** anywhere.
4. **Check the conflict matrix** before claiming. Several items touch
   `DESIGN_DOCUMENTATION.adoc`; two agents editing it at once will collide.
5. **Line numbers drift.** Every item also gives an exact quoted anchor. Match
   on the quoted text, not the line number, once edits start landing.
6. **Re-run the relevant gate** after an item, not the whole suite. Item-level
   verification commands are given per item.

### Conflict matrix

| File | Items that touch it |
| --- | --- |
| `DESIGN_DOCUMENTATION.adoc` | **A1, A2, A3, A5, A7** — serialize these |
| `README.md` | **A4, A5** |
| `release/README.md` | **A5** |
| `test/README.md` | **A6** |
| `docs/pic10f320_*.md` | **A5, D2** (+ A2 touched `_feasibility.md:251`) |
| `.github/workflows/ci.yml` | **C1** |
| `scripts/make-release.sh` | **C2** |
| `.gitignore`, `release/.gitignore` | **C3** |
| `test/pic10f320/**` | **B2, D1** |
| `test/pic/*.cc` | **D1** |
| `src/bypass_mcu_avr_xt.c` | **B1** (user only) |

Fully independent, safe to run in parallel: **A6, B2, C1, C2, C3**.

---

## Verified baseline (do not re-derive)

Established at `af6120a` and unchanged by `d3ba040`. Recorded so nobody spends
another 40 minutes on it.

All seven release qualification gates were re-run locally with the complete
toolchain and **all seven passed**:

```
PASS: test-long
PASS: attiny202-test
PASS: attiny202-test-target
PASS: pic-test
PASS: pic-test-target-variants
PASS: pic320-test
PASS: pic320-test-target-variants
```

Also confirmed against live output rather than documentation:

- mutation: **93 killed, 0 survived, 0 errored, 0 skipped**
- `RELEASE_IMAGES` resolves to exactly **18** images
- release evidence inventory is exactly **28** files (13 fixed + 15 soak), and
  `scripts/make-release.sh` writes all 13 fixed ones
- ATtiny202 fault matrix: **22/22 injections, 23/23 results**
- PIC10F320 equivalence: **266,144 sequences, 66 reachable model states**
- AVR Classic stack high-water: **29 B** (cd4053), **31 B** (relay, mute) —
  the design doc's SRAM prose is **correct**, only its flash tables are not
- no orphaned files survived the merge; every make-target reference in the
  *live* docs resolves

The local host has the full release toolchain (avr-gcc 7.3.0, binutils-avr,
simavr, XC8 3.10 at `/opt/microchip/xc8/v3.10/bin/xc8-cc`, PIC10-12Fxxx DFP
1.9.189, gpsim 0.32.1 + gpsim-dev, vendored ATtiny_DFP, patched yasimavr venv,
cbmc, cppcheck, clang-tidy).

**Gotcha:** complete Make invocations hold a worktree `flock`. Any second `make`
call — including a trivial `make -s print-VAR` — blocks until the first
finishes. While a long gate runs, read Makefile variables with `grep`/`sed`, not
with `print-` targets.

---

# A. Documentation correctness (user-visible)

## A1 — AVR Classic flash table is stale on all nine rows

**Status:** DONE — edit applied to working tree, awaiting user commit
**Priority:** HIGH — published numbers, and they contradict our own last release
**File:** `DESIGN_DOCUMENTATION.adoc`, table `.Table Flash Utilization by Variant (measured, `avr-size`)` (~line 681)

### Evidence

Documented values vs. measured at `d3ba040`:

| Part | Variant | Doc says | Actually | Doc % | Actual % |
| --- | --- | --- | --- | --- | --- |
| ATtiny13a | cd4053 | 684 B | **716 B** | 67% of 1 KB | **69.9%** |
| ATtiny13a | relay | 724 B | **756 B** | 71% | **73.8%** |
| ATtiny13a | mute | 732 B | **756 B** | 72% | **73.8%** |
| ATtiny45 | cd4053 | 710 B | **742 B** | 17% of 4 KB | **18.1%** |
| ATtiny45 | relay | 750 B | **782 B** | 18% | **19.1%** |
| ATtiny45 | mute | 758 B | **782 B** | 19% | **19.1%** |
| ATtiny85 | cd4053 | 710 B | **742 B** | 9% of 8 KB | **9.1%** |
| ATtiny85 | relay | 750 B | **782 B** | 9% | **9.5%** |
| ATtiny85 | mute | 758 B | **782 B** | 9% | **9.5%** |

Two extra consequences worth noting in the fix:

1. The doc's implied ordering **relay (724) < mute (732) is wrong** — relay and
   mute are now byte-identical in size (756 / 782). Any prose that leans on
   "mute is the largest variant" needs to go.
2. `release/v0.9.5/MANIFEST.md` **already published 716 / 756 / 756 and 742 /
   782 / 782**. So the design doc has been contradicting our own shipped release
   manifest since v0.9.5. That is the strongest argument for fixing it now.

### Fix

Replace the nine data rows with the measured values. The table header says
"measured, `avr-size`", so prefer `avr-size`'s own one-decimal percentages over
the current integer ones — that makes the table reproducible by the command in
its own caption.

### Verify

```sh
make clean && make all13 all45 all85
for e in build_avr_classic/bypass_*.elf; do
  b=$(basename "$e" .elf)
  case $b in *_t85) m=attiny85;; *_t45) m=attiny45;; *) m=attiny13a;; esac
  printf '%-28s %s\n' "$b" "$(avr-size --mcu=$m -C "$e" | awk '/^Program:/{print $2" B "$3$4}')"
done
```

### Resolution (2026-07-29)

Re-measured on a clean rebuild before editing; output matched the evidence table
above exactly (716/756/756, 742/782/782, 742/782/782 at 69.9/73.8/73.8,
18.1/19.1/19.1, 9.1/9.5/9.5%). All nine rows replaced, integer percentages
swapped for `avr-size`'s one-decimal values.

Swept for collateral damage, all clean:

- The stale numbers appear nowhere else in the live tree. The only other hits
  are frozen `release/v0.9.1`–`v0.9.4` MANIFESTs and evidence logs, which are
  historical artifacts and must stay as-is.
- No live prose depends on the old `relay < mute` ordering. (`docs/pic10f320_merge_plan.md:746`
  says "the largest variant has 13 words of headroom", but that is PIC10F320,
  unrelated, and belongs to **D2**.)
- The SRAM/stack paragraph directly below the table (4 B static, 31 B / 29 B
  peak stack, ~29 B tiny13a headroom) was already verified correct — untouched.
- "the ATtiny85 uses approximately 9% of flash" still holds (9.1–9.5%).

**Root cause worth remembering:** nothing ties this table to a build. `grep -rn
DESIGN_DOCUMENTATION` finds no test, script or CI job that reads it, so it can
drift silently — and did, for at least three releases. See **A7**, filed below.

---

## A2 — PIC10F322 program-space column is stale (data column is fine)

**Status:** DONE — edits applied to working tree, awaiting user commit
**Priority:** HIGH
**File:** `DESIGN_DOCUMENTATION.adoc`, table `.Table PIC10F322 Utilization by Variant (measured, XC8 V3.10)` (~line 745) plus the prose at ~line 758

### Evidence

Measured with XC8 V3.10 + PIC10-12Fxxx DFP 1.9.189 at `d3ba040`:

```
cd4053:  Program space used 1BDh (445) of 200h words (86.9%)   Data 22h (34) of 40h bytes (53.1%)
relay:   Program space used 1D9h (473) of 200h words (92.4%)   Data 22h (34) of 40h bytes (53.1%)
mute:    Program space used 1D7h (471) of 200h words (92.0%)   Data 22h (34) of 40h bytes (53.1%)
```

| Variant | Doc program | Actual | Doc % | Actual % |
| --- | --- | --- | --- | --- |
| cd4053 | 404 words | **445** | 78.9% | **86.9%** |
| relay | 431 words | **473** | 84.2% | **92.4%** |
| mute | 434 words | **471** | 84.8% | **92.0%** |

**The Data (RAM) column — 34 bytes / 53.1% — is correct. Do not touch it.**
Only the two program-space cells per row are wrong. Note the doc's relay/mute
program ordering is also inverted: mute (471) is now *smaller* than relay (473).

Cross-check: `release/v0.9.5/evidence/build-pic.log` already recorded
445 / 471 / 473, confirming the doc — not the build — is what drifted.

### Fix

Correct the three program-space cells and their percentages.

Then fix the prose immediately below the table:

> "The PIC10F322 leaves comfortable headroom against its 512-word limit, which
> `make pic` gates on every build."

At 473/512 the largest variant has **39 words (7.6%) free**. "Comfortable
headroom" is no longer an accurate characterisation — reword to state the actual
margin. (Unrelated: the "~42% utilisation — comfortable headroom" phrase further
down in *PIC Power / Current Draw* is about **CPU cycle** utilisation, ~210 of
500 cycles/ms. That one is correct — leave it alone.)

### Verify

```sh
cd "$(mktemp -d)"
R=/mnt/home/matt/development/github/mcu-bypass-firmware
for p in "cd4053:OUTPUT_CD4053_SIMPLE:bypass_output_cd4053_simple.c" \
         "relay:OUTPUT_TQ2_L2_5V_RELAY:bypass_output_tq2_l2_5v_relay.c" \
         "mute:OUTPUT_CD4053_WITH_MUTE:bypass_output_cd4053_with_mute.c"; do
  v=${p%%:*}; r=${p#*:}; m=${r%%:*}; drv=${r##*:}
  echo "### $v"
  /opt/microchip/xc8/v3.10/bin/xc8-cc -mcpu=10F322 \
    -mdfp=/opt/microchip/mdfp/PIC10-12Fxxx_DFP/1.9.189/xc8 -std=c99 -O2 \
    -DBYPASS_MCU_PIC10F322 -D_XTAL_FREQ=2000000UL -D$m \
    $R/src/bypass_mcu_pic10f322.c $R/src/bypass_pure.c $R/src/$drv -o $v.hex 2>&1 \
    | grep -iE 'Program space|Data space'
done
```

Note `_XTAL_FREQ` **must** be `2000000UL` — the shell has a `static_assert` that
rejects anything else, and `make pic` filters XC8's memory summary out of its
own output, which is why the direct invocation is needed.

**For reference: the ATtiny202 and PIC10F320 tables were both checked and are
exactly correct** (848/888/888 B at 41.4%/43.4%/43.4%; 220/241/244 words at
85.9%/94.1%/95.3%). Both were written recently. Do not "fix" them.

### Resolution (2026-07-29)

Re-ran the direct XC8 invocation above before editing; output matched the
evidence block exactly (445/473/471 words at 86.9/92.4/92.0%, data 34 B / 53.1%
on all three). Applied:

1. **Table** — the three program-space cells and their percentages corrected.
   The Data column was left byte-for-byte alone, as specified.
2. **Prose below the table** — "leaves comfortable headroom against its 512-word
   limit" replaced with the actual margins (relay 39 words / 7.6% free, cd4053
   67 / 13.1%) and an explicit "enough for maintenance, not for a substantial
   new feature". Kept and sharpened the `make pic` gating claim, which is
   accurate: `Makefile:700` sets `PIC_FLASH_WORDS ?= 512` and the per-variant
   check at `Makefile:881` fails the build on overshoot.

**Second instance of the same false claim, also fixed:**
`docs/pic10f320_feasibility.md:251` asserted "PIC10F322 … all three variants fit
with comfortable headroom. **Still true.**" That is a decision-record entry whose
"Still true" annotations are current-state claims about the repo, so it was
actively republishing the overstatement A2 exists to remove. Reworded to keep
the "it fits" verdict (which *is* still true) while stating the 473/512 margin.
This file is otherwise historical and was not touched further — its disposition
question belongs to **D2**.

Swept clean: the stale figures 404/431/434 and 78.9/84.2/84.8% appear **nowhere**
else in the live tree. The `~42% utilisation — comfortable headroom` phrase at
`DESIGN_DOCUMENTATION.adoc:809` is CPU-cycle utilisation and was left alone, as
instructed.

**Cross-check confirms the direction of the drift:**
`release/v0.9.5/evidence/build-pic.log` records
`445 words (86.9%)`, `471 words (92.0%)`, `473 words (92.4%)` — so the shipped
release evidence has been right all along and only the design doc was wrong.
Same failure mode as A1; see **A7** for why nothing caught it.

---

## A3 — BOD/BOR failsafe list omits two of six release parts (safety-relevant)

**Status:** DONE — edit applied to working tree, awaiting user commit
**Priority:** HIGH — this is the one item with a real-world safety edge
**File:** `DESIGN_DOCUMENTATION.adoc`, *Failsafe Mechanisms*, the `[horizontal]`
definition list (~lines 196–207)

### Evidence

The list currently has exactly two entries:

```
AVR Classic (ATtiny13a/45/85):: 4.3v BOD fuse -- enforces the >4v
  floor the 5v relay coil / MOSFET peripherals require.
PIC10F322:: BOR is enabled at the highest available trip
  (`BORV=HI`), but the PIC10F322's trip points are only ~2.4v (LO) /
  ~2.7v (HI). ...
```

Both **ATtiny202 and PIC10F320 are missing**, and both are release-supported
parts in the 18-image v0.9.6 product set. The section's own framing — "the
BOD/BOR floor is MCU-family-specific and not every supported part can enforce
the >4v peripheral-safe threshold" — promises per-part coverage it doesn't
deliver.

**PIC10F320 (the safety-relevant omission).** `src/bypass_mcu_pic10f320.c` sets
`#pragma config BOREN = ON`, `LPBOR = OFF`, `BORV = HI` — identical to the
PIC10F322. Same silicon family, same ~2.4 V (LO) / ~2.7 V (HI) trip points. It
therefore carries **exactly the same caveat**: it cannot enforce a >4 V floor in
firmware, so a PIC10F320 build driving a 5 V relay coil or MOSFET must guarantee
the peripheral-safe supply in hardware. The 322 says this; the 320 doesn't.

**ATtiny202 (the omission that undersells us).** `XT_FUSE_BODCFG ?= 0xE5`
(`Makefile`). Decoded: `LVL = (0xE5 >> 5) & 7 = 7`, `SAMPFREQ = 0`,
`ACTIVE = 1 (ENABLED)`, `SLEEP = 1 (ENABLED)`. Cross-checked against the pinned
device pack, `third_party/attiny_dfp/include/avr/iotn202.h:842-845`:

```c
LVL_BODLEVEL0_gc = (0x00<<5),  /* 1.8 V */
LVL_BODLEVEL2_gc = (0x02<<5),  /* 2.6 V */
LVL_BODLEVEL7_gc = (0x07<<5)   /* 4.2 V */
```

So the ATtiny202 runs BODLEVEL7 = **4.2 V, BOD active in both run and sleep** —
it *does* enforce the >4 V peripheral-safe floor, and it is the AVR-XT analogue
of the classic 4.3 V BOD. That is good news currently going unstated.

### Fix

Add two entries to the same `[horizontal]` list, matching the existing style:

- `ATtiny202 (AVR-XT)::` — BODCFG `LVL=BODLEVEL7` (~4.2 V), ACTIVE and SLEEP
  both enabled; enforces the >4 V floor, the AVR-XT analogue of the classic
  4.3 V BOD.
- `PIC10F320::` — same silicon family and same `BOREN=ON`/`BORV=HI`
  configuration as the PIC10F322, therefore the **same ~2.7 V ceiling and the
  same hardware-design caveat**.

Keep the PIC10F320 wording as explicit as the 322's. Do not merge the two PIC
entries into one line unless the merged line names both parts — per the
`pic-naming-322-vs-32x` convention, a note that applies to both parts should say
so rather than silently generalising from "322".

### Resolution (2026-07-29)

Both fuse claims re-verified from primary sources before writing any
safety-relevant prose. The list now has four entries, one per MCU family.

**ATtiny202 — decode confirmed against the pinned pack, not the datasheet.**
`Makefile:1782` sets `XT_FUSE_BODCFG ?= 0xE5`. The bit positions come from
`third_party/attiny_dfp/include/avr/iotn202.h:3168-3183`:

```
FUSE_LVL_gm  0xE0, FUSE_LVL_gp  5
FUSE_ACTIVE_gm 0x0C, FUSE_ACTIVE_gp 2
FUSE_SLEEP_gm  0x03, FUSE_SLEEP_gp  0
FUSE_SAMPFREQ_bm 0x10
```

0xE5 = `1110_0101` decodes to LVL=7, SAMPFREQ=0, ACTIVE=1, SLEEP=1. Against
`LVL_enum` (`:839-844`) and `ACTIVE_enum` / `SLEEP_enum`: **BODLEVEL7 = 4.2 V,
BOD ENABLED in active mode, ENABLED in sleep.** BODLEVEL7 is one of only three
enumerated levels (1.8 / 2.6 / 4.2 V), so it is characterised, not reserved.

> **Trap for anyone re-checking this:** `iotn202.h` has *two* BOD level enums.
> `BOD_LVL_enum` (`:310-315`) shifts by **0** — that is the runtime `BOD.CTRLB`
> register. The fuse enum `LVL_enum` (`:839-844`) shifts by **5**. Decoding the
> BODCFG *fuse* byte with the register enum gives a wrong answer. Use
> `FUSE_LVL_gp = 5`.

**PIC10F320 — confirmed identical to the 322.** `src/bypass_mcu_pic10f320.c:108,114,115`
sets `BOREN=ON`, `LPBOR=OFF`, `BORV=HI`; `src/bypass_mcu_pic10f322.c:53,59,60`
sets exactly the same three. Same silicon family, same ~2.4 V / ~2.7 V trips,
same inability to enforce a >4 V floor.

Wording choices, per the constraint above: the two PIC parts are kept as
**separate entries**, and the shared hardware-design caveat sentence in the
PIC10F322 entry was changed from "A **PIC build** that drives >4v peripherals"
to "A build on **either PIC part** that drives >4v peripherals" so the shared
advice names both parts instead of generalising from "322".

Swept: this `[horizontal]` list is the only per-part BOD/BOR coverage claim in
the live tree. Other hits (`test/README.md:440-442` gpsim analog-BOR gap,
`docs/phase2_pic_shell.md:85` config-bit list, `TODO.md` items) are correctly
scoped already. AsciiDoc structure unchanged — same `[horizontal]` block, two
more `::` entries; `asciidoctor` is not installed here so no render check was
possible, but no block delimiters were touched.

### Follow-on found while verifying (not fixed — firmware)

`src/bypass_mcu_pic10f322.c:32-36` carries an explicit "BOR note" comment
explaining the ~2.4/~2.7 V limitation. **`src/bypass_mcu_pic10f320.c` has the
identical `#pragma config` block with no such note** — a maintainer reading only
the 320 shell gets the same configuration with none of the safety reasoning.
This is a firmware comment edit, so per `AGENTS.md` it is the user's. Suggest
folding it into **B1**, which is already a `NEEDS-USER` firmware-comment item.

---

## A4 — README simulator sentence omits AVR-XT lock-step

**Status:** TODO
**Priority:** MEDIUM
**File:** `README.md`, *Testing and Validation Features* bullet (~lines 76–81)

### Evidence

Current text:

> "Built-image simulator validation: simavr for AVR Classic and gpsim/libgpsim
> for PIC10F322 provide functional, fault-injection, **lock-step**, target-I/O,
> and soak tests; yasimavr for AVR-XT (ATtiny202) provides functional,
> fault-injection, physical target-output timing, and soak tests."

The PIC list names lock-step; the AVR-XT list doesn't — yet `make
attiny202-lockstep` exists, runs in normal CI, and is part of the fail-closed
`attiny202-test-target` release aggregate. It passed in the baseline run above
(`LOCKSTEP PASS: 0 check(s) failed`).

`CHANGELOG.md` calls this gate out as closing "the last structural verification
gap" — the README undersells the project's own headline addition.

### Fix

Add lock-step to the AVR-XT clause. Suggested: "...provides functional,
fault-injection, lock-step, physical target-output timing, and soak tests."

---

## A5 — Flip post-release status language

**Status:** TODO
**Priority:** MEDIUM — do this **after** the v0.9.6 tag is published
**Files:** several — see table

### Context (important)

The release workflow requires the tagged release commit to be a single-parent,
**artifact-only** child of the qualified source commit, touching only
`release/<version>/`. That is why this could not be folded into the release
commit. The release was started from a separate clone at a commit that still
carries "pending" language, so **the published `v0.9.6` tag will contain the
stale text**. That cannot be changed now and is cosmetic — release consumers
read `release/README.md` and the per-release `MANIFEST.md`, both generated or
maintained separately.

The job here is to make `main` correct going forward.

### Sites (verified present at `d3ba040`)

| File | Anchor |
| --- | --- |
| `README.md` ~6–11 | "Published releases through `v0.9.5` predate…the first unified release covering all six **remains pending** production qualification on the final source (a prior full-tool dry rehearsal passed but is not publishable)." |
| `README.md` ~19 | ATtiny202 row: "**first release pending**" |
| `README.md` ~21 | PIC10F320 row: status cell "**integrated release candidate**" and "**first unified release pending**" |
| `release/README.md` ~10–15 | "**Current availability:** committed releases stop at `v0.9.5` and contain neither PIC10F320 nor ATtiny202 images…" |
| `DESIGN_DOCUMENTATION.adoc` ~902 | "the PIC10F320's first unified release is …" |
| `docs/pic10f320_feasibility.md` ~18 | "Its first unified release remains …" |
| `docs/pic10f320_special_case.md` ~3–6, ~199 | "**Status:** integrated and wired into fail-closed release gates, but its first unified release is pending production qualification…" |
| `docs/pic10f320_validation.md` ~23–29 | "the unified tag workflow has not published a release" |
| `TODO.md` ~314 | "no unified release manifest …" |
| `CHANGELOG.md` | `## [Unreleased]` → `## [0.9.6] - <date>`, and open a fresh empty `[Unreleased]` |

### Decision needed from the user

`README.md` currently distinguishes **ATtiny202 = "release-supported"** from
**PIC10F320 = "integrated release candidate"**. Once v0.9.6 ships, both have
been through *identical* production qualification and both have images in the
same release. Either:

- **(a)** promote PIC10F320 to "release-supported" and keep the constrained-
  exception caveat in the Notes cell (recommended — the status column then means
  one consistent thing), or
- **(b)** keep the two-tier status and add a sentence saying explicitly what the
  lower tier now means post-qualification.

Ask before choosing; do not silently pick one.

### Verify

```sh
grep -rn 'remains pending\|first release pending\|integrated release candidate\|stop at .v0\.9\.5' \
  README.md release/README.md DESIGN_DOCUMENTATION.adoc docs/ TODO.md
```

Expect zero hits outside `docs/pic10f320_merge_plan.md` (historical, see D2) and
`release/v0.9.*/` (frozen published artifacts — **never edit those**).

---

## A6 — `test/README.md` root inventory is incomplete

**Status:** TODO
**Priority:** LOW
**File:** `test/README.md`, the ``` ``` ``` layout block

### Evidence

The `test/` root block reads as an exhaustive inventory (every entry tagged
`shared:`) but omits nine tracked files. Checked at `d3ba040`:

Genuinely missing from the root `shared:` list:

- `test/mutation_accounting.sh`
- `test/test_target_matrix.sh`
- `test/test_target_lane_markers.sh`
- `test/test_lockstep_progress.sh`
- `test/test_pic_rebuild.sh`
- `test/test_stack_depth_pic.sh`
- `test/check_stack_depth_pic.sh`

Missing from the `avr/` subsection:

- `test/avr/attiny202_smoke.c`
- `test/avr/test_attiny202_delay_oracle.py`

Several of these are substantial gates (`mutation_accounting.sh`,
`test_target_matrix.sh`, `test_target_lane_markers.sh`), not trivia.

**Not** defects — already covered by wildcard/directory lines, do not "fix":
`test/pic/power_on_pressed.stc` and `run_gpsim_*.sh` (covered by
`*.stc + run_gpsim_*`), `test/pic/fw_coverage/*` and
`test/pic10f320/fault/fw_fault_harness.*` (covered by their directory lines),
`test/avr/model_step_ffi.py` (covered by `model_step_ffi.c/.py`),
`test/pic/find_pin_exact.h` (judgment call — it is shared across both PIC
chips' harnesses and arguably deserves a line).

### Verify

```sh
for f in $(git ls-files test/ | grep -vE '\.gitignore|README'); do
  grep -qF "$(basename "$f")" test/README.md || echo "MISSING: $f"
done
```

---

## A7 — "large headroom on every supported part" is false for the PIC parts

**Status:** TODO
**Priority:** MEDIUM
**File:** `DESIGN_DOCUMENTATION.adoc`, `== Resource Utilization` opening
sentence (immediately above the `=== AVR Classic family` heading)
**Found by:** A1, while sweeping for prose that depended on the stale numbers.

### Evidence

Anchor text:

> The firmware is deliberately tiny, leaving large headroom on every supported
> part. Measured figures per MCU family follow.

Against the *correct* numbers in the tables further down the same section:

| Part | Worst variant | Flash used |
| --- | --- | --- |
| PIC10F320 | mute | **95.3%** of 256 words |
| PIC10F322 | relay | **92.4%** of 512 words |
| ATtiny13a | relay/mute | **73.8%** of 1 KB |
| ATtiny202 | relay/mute | 43.4% of 2 KB |
| ATtiny45 | relay/mute | 19.1% of 4 KB |
| ATtiny85 | relay/mute | 9.5% of 8 KB |

"Large headroom on **every** supported part" is contradicted two screens later
by the document's own PIC10F320 table, where the design is 12 words from the
flash ceiling — the entire reason `docs/pic10f320_special_case.md` exists. This
is the sentence a sceptical reader hits first, so it is worth more than its size.

### Fix

Rewrite the universal claim as a range plus the named exception. Something like:
headroom is generous on the roomier parts and deliberately tight on the
256-word PIC10F320, where the hand-inlined build is documented as a special
case. Do not touch any table; A1/A2 already corrected those, and the ATtiny202
and PIC10F320 tables were verified correct and must be left alone.

Related, same section, lower priority: the paragraph after the AVR Classic
table says the headroom "leaves ample room for future features without
approaching any resource limit". Scoped to AVR Classic that is defensible
(73.8% worst case), so it is not part of this item — but if the fix above
rewords the section intro, check the two read consistently.

### Verify

Read-only. No build or test depends on this prose.

### Note on why this class of bug survives

`grep -rn DESIGN_DOCUMENTATION Makefile scripts/ test/ .github/workflows/`
returns only three incidental mentions — a release-notes string and two
comments. **Nothing reads the document's numbers**, so no gate can notice when
they drift from the build; A1's table had been contradicting our own shipped
`release/v0.9.5/MANIFEST.md` since v0.9.5. A future TODO.md Tier item worth
filing: have a test extract the utilisation tables and diff them against
`avr-size` / XC8 output, the way `mutation_accounting.sh` pins the mutant count.
That is a new test, not a v0.9.6 polish item — it should not be attempted on
this branch.

---

# B. Source and comment defects

## B1 — Unresolved `CONFIRM` note in shipping ATtiny202 firmware

**Status:** NEEDS-USER
**Priority:** MEDIUM
**File:** `src/bypass_mcu_avr_xt.c` (~lines 39–44) — **firmware; user edit only**

### Evidence

The fuse-table comment block carries an open verification instruction, on a
safety-relevant fuse, in a part just promoted to release-supported:

```
//   BODCFG   | LVL=BODLEVEL7 (~4.2V),        | Peripheral-safe floor: relay/MOSFET need
//            | ACTIVE=ENABLED, SLEEP=ENABLED | >4V. ~4.2V is the AVR-XT analogue of the
//            |                               | classic 4.3V BOD.  CONFIRM the level
//            |                               | encoding + that it is a characterised
//            |                               | (non-reserved) level in the ATtiny202
//            |                               | datasheet Electrical Characteristics.
```

**This has now been confirmed** — see A3 for the full derivation. Summary:
`XT_FUSE_BODCFG = 0xE5` decodes to `LVL=7`, and the pinned device pack
(`third_party/attiny_dfp/include/avr/iotn202.h:842-845`) enumerates exactly
three characterised levels — BODLEVEL0 (1.8 V), BODLEVEL2 (2.6 V), BODLEVEL7
(4.2 V). Level 7 is characterised, not reserved, and is the highest available.
The encoding and the value are both correct.

### Suggested change (for the user to make)

Replace the `CONFIRM …` sentence with the resolved statement, citing the pack so
the evidence travels with the code. Something like:

```
//            |                               | classic 4.3V BOD.  Level encoding
//            |                               | confirmed against the pinned
//            |                               | ATtiny_DFP (iotn202.h LVL_enum):
//            |                               | BODLEVEL0/2/7 = 1.8/2.6/4.2V are the
//            |                               | only characterised levels; 7 is the
//            |                               | highest and is not reserved.
```

Keep it to a comment change — **no functional change is needed or implied.**
`XT_FUSE_BODCFG` stays `0xE5`.

### Verify

Comment-only, so no behavioural gate applies. Confirm nothing moved:

```sh
make attiny202-test STRICT_TOOLS=1
```

---

## B2 — Imported PIC10F320 harnesses name the child project's make targets

**Status:** DONE 50db549
**Priority:** MEDIUM — these names do not exist; a reader following them gets an error
**Files:** four files under `test/pic10f320/`

### Evidence

Verified at `d3ba040` that none of the cited targets exist and that the
replacements do. These are leftovers from the separate repository the PIC10F320
was merged from, where the lanes had un-prefixed names.

| File:line | Stale text | Correct target |
| --- | --- | --- |
| `test/pic10f320/equiv/xc.h:4` | `(make test-equiv)` | `pic320-test-equiv` |
| `test/pic10f320/gpsim/footswitch_toggle.stc:14` | ``` `make test-gpsim` ``` | `pic320-test-gpsim` |
| `test/pic10f320/gpsim/test_fault_pic.cc:76` | ``` `make test-fault-gpsim` ``` | `pic320-test-fault-target` |
| `test/pic10f320/gpsim/test_fault_pic.cc:79` | ``` `make test-target-variants` ``` | `pic320-test-target-variants` |
| `test/pic10f320/gpsim/test_lockstep_pic.cc:50` | ``` `make test-lockstep-gpsim` ``` | `pic320-test-lockstep` |
| `test/pic10f320/gpsim/test_lockstep_pic.cc:53` | ``` `make test-target-variants` ``` | `pic320-test-target-variants` |

**Plus a separate factual error in the same file:**

`test/pic10f320/gpsim/footswitch_toggle.stc:16` says:

> "The HEX (any of the **five** output variants) is supplied on the gpsim
> command line…"

The PIC10F320 has **three** supported variants —
`PIC320_VARIANTS_SUPPORTED := cd4053-simple cd4053-mute tq2-relay`
(`Makefile:16`). "Five" is inherited from the child project's older matrix.

Note the same file's reference to the wrapper `test/pic/run_gpsim_test.sh` **is
correct** — the wrappers were genuinely folded during the merge and are now
shared by both PIC parts. Do not "fix" that one.

### Fix

Comment/text edits only, in test files (allowed). No behavioural change.

### Verify

```sh
# no stale child-project target names remain
grep -rn 'make test-equiv\|make test-gpsim\|make test-fault-gpsim\|make test-lockstep-gpsim\|make test-target-variants\|five output variants' test/pic10f320/
# and the lanes still run
make pic320-test-target-variants STRICT_TOOLS=1
```

---

# C. Build, CI and release machinery

## C1 — ATtiny202 CI job derives its expected counts from an overridable variable

**Status:** TODO
**Priority:** MEDIUM — this is the exact failure class `RELEASE_IMAGES` exists to prevent
**File:** `.github/workflows/ci.yml`, job `attiny202`

### Evidence

Four steps size their expectation from `make -s print-VARIANTS` — the
**AVR Classic**, user-overridable variant list — rather than from the ATtiny202's
own immutable `XT_VARIANTS_SUPPORTED`:

| Line (at `d3ba040`) | Step |
| --- | --- |
| 439 | "Assert ATtiny202 images built (fail loud, do NOT skip)" — `for v in $(make -s print-VARIANTS)` |
| 493 | "Functional + target-output gate" — `n=$(make -s print-VARIANTS \| wc -w)` |
| 517 | "Lock-step gate" — `n=$(make -s print-VARIANTS \| wc -w)` |
| 531 | "Soak smoke" — `n=$(make -s print-VARIANTS \| wc -w)` |

The fault-injection step at **line 503** already does the right thing:

```yaml
# Pin the complete development matrix independently of Make defaults;
# each FAULT PASS also requires 22/22 injections, 23/23 results, no skips.
n=3
```

So the hazard was recognised and fixed in exactly one of five steps.

**Why it matters.** A `VARIANTS=cd4053` override would make four of these gates
expect one image / one PASS and go green having exercised a third of the matrix.
This is the same "three observations derived the same wrong way agree perfectly"
problem the `RELEASE_IMAGES` design comment (Makefile ~line 4365) was written to
kill — left in the one lane it wasn't applied to. It is also the *wrong variable
family*: `VARIANTS` is the classic-AVR list; the two happen to be identical
(`cd4053 mute relay`) today, so nothing is broken right now.

### Fix

Two options — pick one and apply it to all four steps consistently:

- **(a)** Use `make -s print-XT_VARIANTS_SUPPORTED` (immutable — it is declared
  with `override`). Keeps the count derived from Makefile truth while making an
  override unable to shrink it.
- **(b)** Hardcode `n=3` with the same justifying comment the fault step uses.

**(a) is preferable** — it cannot drift if a fourth ATtiny202 variant is ever
added, whereas (b) would silently under-count. If (a) is chosen, consider
whether the fault step's `n=3` should also become `print-XT_VARIANTS_SUPPORTED`
for uniformity, keeping its comment.

### Verify

```sh
make test-workflow-syntax
scripts/ci-local.sh --help   # confirm the job map still agrees
```

---

## C2 — `MANIFEST.md` carries a repo-relative link but is published as release notes

**Status:** TODO
**Priority:** MEDIUM
**File:** `scripts/make-release.sh` (~line 1024)

### Evidence

The generator emits:

```sh
printf 'Full detail: [docs/pic10f320_special_case.md](../../docs/pic10f320_special_case.md).\n\n'
```

`../../` is correct for the committed file at `release/<version>/MANIFEST.md`
resolving to the repo root. But the same file is used verbatim as the GitHub
Release body — `.github/workflows/release.yml` (~line 319):

```sh
notes="$dir/MANIFEST.md"; [ -f "$notes" ] || notes=/dev/null
...
gh release create "$tag" --notes-file "$notes" ...
```

On the release page that relative path does not resolve to the document.

**This is a regression against v0.9.5 specifically.** `v0.9.3`–`v0.9.5`
manifests contain no relative links; `v0.9.0`–`v0.9.2` did (`../README.md`), and
those were retro-added to the committed file after publication, so there is no
published precedent to inspect. The v0.9.6 manifest is the first to ship one.

### Fix

Make the link absolute so it is correct in **both** contexts — the committed
file and the release page:

```
https://github.com/matt-garman/mcu-bypass-firmware/blob/<tag>/docs/pic10f320_special_case.md
```

`$VERSION` is already in scope in the generator, so the tag can be interpolated,
which also pins the link to the matching source revision rather than to a moving
`main`.

Check whether the sibling emission at ~line 1110 —
`[release/README.md](../README.md)` — needs the same treatment. It goes into
`release/<version>/README.md`, which is **not** uploaded as a release asset, so
it is probably fine as-is; confirm rather than assume.

### Verify

Regenerate a manifest without a full run and eyeball the link:

```sh
grep -n 'special_case.md\](' scripts/make-release.sh
make test-release-qualification
```

---

## C3 — The two `.gitignore` files contradict each other about `commit_msg.txt`

**Status:** TODO
**Priority:** LOW
**Files:** `.gitignore` (~lines 56–59), `release/.gitignore`

### Evidence

Root `.gitignore` says:

```
# The commit message the release script and the development workflow generate
# for the human to use with `git commit -F`. A working note, never an artifact.
# Anchored to the root: release/<ver>/commit_msg.txt IS tracked, deliberately.
/commit_msg.txt
```

But `release/.gitignore` is exactly one line:

```
commit_msg.txt
```

…which ignores it in every release directory. Verified:

```
$ git ls-files | grep -i commit_msg    ->  (nothing)
$ git status --ignored                 ->  release/v0.9.3/commit_msg.txt
                                           release/v0.9.5/commit_msg.txt
```

One was tracked historically (added in `de261b6`, "release: firmware v0.9.0")
and later removed, which is presumably where the comment's claim came from.

The comment states the opposite of actual behaviour, and it is load-bearing —
it is the stated *reason* the root pattern is anchored with a leading `/`.
Anyone reading it would believe release commit messages are archived. They are
not.

### Fix

Decide which behaviour is intended, then make the two files agree:

- **(a)** Behaviour is right, comment is wrong → correct the comment to say the
  root anchor exists so the dev-time working note is ignored without affecting
  anything else, and that `release/.gitignore` deliberately drops the
  per-release copies too. *(Lowest risk; recommended.)*
- **(b)** Comment describes the intent → delete `release/.gitignore` and
  actually track `release/<ver>/commit_msg.txt`. Note this changes what the
  release commit contains; it stays inside `release/<version>/`, so it would not
  violate the artifact-only-child rule the tag CI enforces — but it is a real
  behavioural change and should be the user's call.

Do not guess. Ask which is intended.

---

# D. Redundancy (judgment calls — discuss before acting)

## D1 — ~1,300 duplicated lines of libgpsim harness across the two PIC parts

**Status:** DONE 48e5b4c
**Priority:** MEDIUM — the single largest redundancy the merge left behind
**Files:** six `.cc` adapters, three shared cores under `test/pic/`, `Makefile`,
`test/README.md`, and the mutation-sandbox inventory

### Evidence

Measured at `d3ba040` with a whitespace-exact common-line diff:

| Harness | PIC10F322 | PIC10F320 | identical lines | 320 copy verbatim |
| --- | --- | --- | --- | --- |
| `test_fault_pic.cc` | 554 | 567 | 470 | **~83%** |
| `test_io_pic.cc` | 320 | 321 | 306 | **~95%** |
| `test_lockstep_pic.cc` | 353 | 414 | 282 | **~68%** |
| **total** | **1,227** | **1,302** | **1,058** | |

2,529 lines of C++ carrying roughly 1,050 lines of duplication.

The merge **did** fold the shell wrappers — `Makefile` ~line 3849 records
`FOLD run_gpsim*.sh -- differed only in the default PROC`, and both parts share
`test/pic/run_gpsim_test.sh` and `run_gpsim_power_on_pressed.sh`. The `.cc`
harnesses were the largest remaining duplicated block.

### Resolution

The user chose the full fold rather than filing a future `TODO.md` item. A
line-by-line executable review refined the initial finding: the I/O and lock-step
harness pairs differ only in adapter configuration and comments. The fault pair
has three real per-part policies: PIC10F322 scans 512 program words and runs 25
checks including three LATA injections; PIC10F320 scans 256 words and runs 22
checks without LATA injections because its firmware deliberately omits the latch
guard for flash budget.

The six original `.cc` paths now remain as thin adapters over three include-only
shared cores in `test/pic/`. The adapters explicitly pin processor/image defaults,
native output-macro vocabulary, and the fault policies above. The Makefile tracks
the cores as dependencies of the PIC10F322 build-only binaries, while the
PIC10F320 phony lanes continue to recompile every invocation. The mutation
sandbox now requires all three cores and rejects a deliberately removed one.

Verification passed with XC8 3.10, PIC10-12Fxxx DFP 1.9.189, and gpsim/libgpsim
0.32.1:

```sh
make test-lockstep-progress                     # 8 checks
make test-mutation-sandbox                      # 30 checks
make test-target-matrix test-target-lane-markers
make test-pic-build test-pic-build-rebuild
make pic-test-target-variants STRICT_TOOLS=1
make pic320-test-target-variants STRICT_TOOLS=1
```

Both complete three-variant target matrices passed with the expected fault,
lock-step, and I/O check counts. The common-mode risk remains inherent in shared
test code, but both independent firmware images exercise every shared core across
their complete variant matrices.

---

## D2 — Disposition of `docs/pic10f320_merge_plan.md`

**Status:** CLAIMED OpenCode
**Priority:** LOW
**File:** `docs/pic10f320_merge_plan.md` (3,380 lines at `d3ba040`)

### Evidence

It is **41% of all project documentation** (3,380 of ~8,250 lines across
`README`/`CHANGELOG`/`TODO`/`DESIGN_DOCUMENTATION`/`TOOLCHAIN`/`MISRA`/`test`/`docs`)
and it describes a migration that is now complete.

It is also where essentially all remaining staleness in the repo is
concentrated. Every dangling path and every undefined make-target reference
found in the whole tree lives in this one file:

- dangling paths: `docs/phase1_hw_abstraction.md`, `docs/phase2b_pic_shell_spec.md`,
  `test/model/README.md`, `test/actuation/test_actuation.c`, `test/equiv/xc.h`,
  `test/fault/test_fault.c`
- undefined targets: `pic320-coverage`, `pic320-coverage-check-model`,
  `pic320-coverage-clean`, `pic320-test-formal`, `pic320-test-mutation`,
  `pic320-test-target-gpsim`, `pic320-test-variants`,
  `pic320-verify-baseline-images`, `test-all-targets`
- ~7 statements asserting ATtiny202 is development-only, patched by a single
  "Scope erratum (2026-07-29)" note at line 32 that the reader must remember
  while reading the other 3,348 lines

By contrast the **live** documentation is clean: every make target cited in
`README.md`, `DESIGN_DOCUMENTATION.adoc`, `docs/pic10f320_special_case.md`,
`docs/pic10f320_validation.md` and `test/README.md` resolves to a real target.

### Counter-argument (why this is not simply "delete it")

It is load-bearing. The `Makefile` cites it by section for rationale —
"merge plan §10, §14.8" at the `RELEASE_IMAGES` definition, "§5.3, decision D2"
for the surviving basename conventions. There is also precedent *both* ways:
`docs/phase1_hw_abstraction.md` and `docs/phase2b_pic_shell_spec.md` were
deleted as obsolete planning docs, yet a decision record is exactly the kind of
artifact a "textbook-grade reference" project should keep.

### Resolution

The user selected the historical-banner option. The plan remains intact so its
section-numbered decision record and implementation citations stay stable, but a
prominent banner now identifies the merge as complete, warns that paths, targets,
scope assumptions, and status statements in the body are historical, and directs
all current status questions to `docs/pic10f320_special_case.md` and
`docs/pic10f320_validation.md`.

---

# Summary board

| ID | Item | Priority | Status |
| --- | --- | --- | --- |
| A1 | AVR Classic flash table stale (9 rows) | HIGH | **DONE** (uncommitted) |
| A2 | PIC10F322 program-space column stale (3 rows) + prose | HIGH | **DONE** (uncommitted) |
| A3 | BOD/BOR list omits ATtiny202 + PIC10F320 | HIGH | **DONE** (uncommitted) |
| A4 | README omits AVR-XT lock-step | MEDIUM | TODO |
| A5 | Flip post-release status language | MEDIUM | TODO (after tag) |
| A6 | `test/README.md` inventory incomplete | LOW | TODO |
| A7 | "large headroom on every part" false for PICs | MEDIUM | TODO (filed by A1) |
| B1 | `CONFIRM` note in ATtiny202 firmware (+ missing BOR note in `bypass_mcu_pic10f320.c`, found by A3) | MEDIUM | NEEDS-USER |
| B2 | Stale child-project target names in `test/pic10f320/` | MEDIUM | DONE 50db549 |
| C1 | CI ATtiny202 counts from overridable `VARIANTS` | MEDIUM | TODO |
| C2 | MANIFEST relative link broken as release notes | MEDIUM | TODO |
| C3 | `.gitignore` self-contradiction | LOW | TODO |
| D1 | Duplicated libgpsim harnesses (~1,050 lines) | MEDIUM | DONE 48e5b4c |
| D2 | Merge-plan doc disposition | LOW | CLAIMED OpenCode |

**Open questions for the user:** A5 (status-tier decision), C3 (which behaviour
is intended).

---

# Appendix — re-derivation commands

Everything above can be re-checked from scratch with these.

```sh
# --- full release qualification, ~40 min, all seven gates ---
for t in test-long attiny202-test attiny202-test-target pic-test \
         pic-test-target-variants pic320-test pic320-test-target-variants; do
  make $t STRICT_TOOLS=1 && echo "PASS: $t" || echo "FAIL: $t"
done

# --- canonical release contract ---
make -s print-RELEASE_IMAGES | tr ' ' '\n' | grep -c hex      # expect 18
make -s print-RELEASE_EVIDENCE_FILES | wc -w                  # expect 28
make -s print-XT_VARIANTS_SUPPORTED                           # cd4053 mute relay
make -s print-PIC320_VARIANTS_SUPPORTED                       # cd4053-simple cd4053-mute tq2-relay

# --- dangling path references across the whole tree ---
git ls-files -z | xargs -0 grep -ohE '\b(src|test|scripts|docs)/[A-Za-z0-9_./-]+' \
  | sed 's/[.,;:)"'"'"']*$//' | sort -u \
  | while read -r p; do [ -e "$p" ] || echo "MISSING: $p"; done

# --- make targets cited in docs but not defined ---
make -qp 2>/dev/null | grep -E '^[a-zA-Z0-9][a-zA-Z0-9_.-]*:' | cut -d: -f1 | sort -u > /tmp/targets
git ls-files '*.md' '*.adoc' | grep -v '^release/' \
  | xargs grep -ohE '\bmake +[a-z][a-z0-9_-]{2,}' | sed 's/^make *//' | sort -u \
  | comm -23 - /tmp/targets

# --- shared PIC harness adapter/core structure ---
for h in fault io lockstep; do
  test -f "test/pic/test_${h}_pic_core.h"
  grep -qF "pic/test_${h}_pic_core.h" "test/pic/test_${h}_pic.cc"
  grep -qF "pic/test_${h}_pic_core.h" "test/pic10f320/gpsim/test_${h}_pic.cc"
done

# --- ATtiny202 BODCFG decode ---
grep -n 'XT_FUSE_BODCFG' Makefile
python3 -c "v=0xE5; print('LVL',(v>>5)&7,'SAMPFREQ',(v>>4)&1,'ACTIVE',(v>>2)&3,'SLEEP',v&3)"
grep -n -A4 'LVL_enum' third_party/attiny_dfp/include/avr/iotn202.h
```
